# 排序演算法分析報告

**學號：** 11428234
**姓名：** 吳宥蓁
**模擬頁面：** [https://ayu0769.github.io/sort_report/](https://ayu0769.github.io/sort_report/)

---

## 前言

本報告以五種經典排序演算法為研究對象：氣泡排序、選擇排序、插入排序、合併排序與快速排序。目標為說明各排序法的基本原理與操作方式，分析它們的時間與空間複雜度，並透過程式模擬比較不同資料規模下的排序效率，最後綜合理論與實驗結果提出心得與結論。

---

## 1. 排序法原理

### 1.1 氣泡排序法 (Bubble Sort)
氣泡排序透過重複掃描陣列，逐一比較相鄰元素。若前者大於後者，則交換位置。每一輪結束後，最大的元素會像氣泡般「浮」到末端。

### 1.2 選擇排序法 (Selection Sort)
選擇排序將陣列分成已排序與未排序兩部分。每一輪在未排序區間中找出最小值，並將該最小值交換到目前未排序區間的第一個位置。

### 1.3 插入排序法 (Insertion Sort)
插入排序把陣列分成已排序區段與未排序區段。每次取出未排序區段的第一個元素，從已排序區段右端開始比較，並插入到正確位置。

### 1.4 合併排序法 (Merge Sort)
合併排序屬於分治法。它先將陣列不斷對半切分，直到每個子陣列只有一個元素，然後再兩兩合併，合併時保持結果有序。

### 1.5 快速排序法 (Quick Sort)
快速排序先選擇一個基準值（pivot），將陣列重新排列成左側小於基準、右側大於基準，接著遞迴對左右子陣列執行相同步驟。

---

## 2. 複雜度分析

| 排序法 | 平均時間複雜度 | 最好時間複雜度 | 最壞時間複雜度 | 空間複雜度 | 穩定性 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 氣泡排序 | $O(n^2)$ | $O(n)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| 選擇排序 | $O(n^2)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 不穩定 |
| 插入排序 | $O(n^2)$ | $O(n)$ | $O(n^2)$ | $O(1)$ | 穩定 |
| 合併排序 | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | $O(n)$ | 穩定 |
| 快速排序 | $O(n \log n)$ | $O(n \log n)$ | $O(n^2)$ | $O(\log n)$ | 不穩定 |


- 氣泡排序、選擇排序、插入排序屬於 $O(n^2)$ 之簡單排序，適合小規模資料。
- 合併排序與快速排序屬於 $O(n \log n)$ 之高效排序，適合較大資料量。
- 空間複雜度方面，合併排序需要額外暫存空間，而其他三種原地排序只需常數空間。

---

## 3. 實驗設計

### 3.1 實驗目的
透過專案中的排序模擬網頁，量化比較不同排序法在資料規模增加時的執行成本，並觀察排序時間與步數的變化。

### 3.2 實驗工具
本報告使用專案中的五個排序視覺化頁面：
- `bubblesort.html`
- `selectionsort.html`
- `insertionsort.html`
- `mergesort.html`
- `quicksort.html`

每個頁面皆可模擬排序步驟，顯示各種統計數據（比較次數、交換次數、分割次數、步數與耗時）。

### 3.3 實驗資料與方法
- 以隨機生成的整數資料集合進行測試。
- 測試資料規模為 n = 10、20、50。
- 每個規模採 20 組隨機資料，計算平均結果。
- 以比較次數、交換次數/插入次數、分割次數與執行步數作為效能指標。

---

## 4. 結果比較

### 4.1 實驗結果表格

| n | 排序法 | 平均比較次數 | 平均交換/插入次數 | 平均分割次數 |
| :---: | :--- | :---: | :---: | :---: |
| 10 | 氣泡排序 | 45.0 | 24.2 | - |
| 10 | 選擇排序 | 45.0 | 7.45 | - |
| 10 | 插入排序 | 30.95 | 24.2 | - |
| 10 | 合併排序 | 23.0 | - | - |
| 10 | 快速排序 | 26.45 | 18.35 | 6.2 |
| 20 | 氣泡排序 | 190.0 | 92.75 | - |
| 20 | 選擇排序 | 190.0 | 15.95 | - |
| 20 | 插入排序 | 109.05 | 92.75 | - |
| 20 | 合併排序 | 64.45 | - | - |
| 20 | 快速排序 | 71.1 | 46.65 | 13.15 |
| 50 | 氣泡排序 | 1225.0 | 626.45 | - |
| 50 | 選擇排序 | 1225.0 | 45.55 | - |
| 50 | 插入排序 | 672.4 | 626.45 | - |
| 50 | 合併排序 | 223.75 | - | - |
| 50 | 快速排序 | 256.35 | 153.2 | 33.15 |

### 4.2 效能觀察
- 隨著 n 增加，三種 $O(n^2)$ 排序的比較次數與交換次數急速上升，尤其是氣泡排序與插入排序。
- 合併排序與快速排序的比較次數增長較緩，符合 $O(n \log n)$ 的趨勢。
- 選擇排序的比較次數與氣泡排序相同，但交換次數較少，因為每輪最多只交換一次。
- 插入排序在 n = 10 時比氣泡排序更優，但隨著 n 增加仍舊落後於合併排序與快速排序。
- 快速排序的分割次數與比較次數均顯示其平均效率較高，但仍受輸入資料排列影響，最壞情況仍可能退化。

### 4.3 n 增加時的變化
- $n = 10$ 到 $n = 20$：氣泡、選擇與插入排序的比較次數大約增加 4 倍，顯示 $O(n^2)$ 的增長特性。
- $n = 20$ 到 $n = 50$：同樣三種排序的比較次數顯著增加，快速排序與合併排序的成長幅度較小。
- 結論：資料規模越大，$O(n \log n)$ 排序相對優勢越明顯。

---

## 5. 心得與結論

